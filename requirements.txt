# Core
sounddevice
numpy
scipy
Pillow
python-dotenv

# Wake word — 两个都装，config 切换
# Sherpa-ONNX：中文原生支持、零训练（默认）
sherpa-onnx
# pypinyin：把中文关键词转成 sherpa KWS 需要的拼音 token
pypinyin
# OpenWakeWord：英文为主，需要训练 onnx（可选）
openwakeword
onnxruntime

# AI APIs
openai
requests

# TTS
edge-tts
# 豆包/火山引擎 TTS 2.0 流式 WebSocket（可选，切到豆包引擎时才用到）
websockets

# Web control panel
fastapi
uvicorn[standard]
python-multipart
